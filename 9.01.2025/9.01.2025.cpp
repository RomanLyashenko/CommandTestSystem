#include <iostream>
#include <string>
#include <fstream>
#include <vector> 
#include "User.h"
using namespace std;

int main()
{
	string a, b, c, d, e;
	cin >> a >> b >> c >> d >> e;
	User user(a, b, c, d, e);
}
